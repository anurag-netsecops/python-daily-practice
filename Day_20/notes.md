# Day 20 Notes

## Concepts Learned

- String Alignment Methods
- `ljust()`
- `rjust()`
- `center()`
- String Multiplication
- `for` Loops
- Pattern Printing

## String Alignment Methods

Python provides built-in methods to align strings.

### `ljust()`

The `ljust()` method aligns a string to the left.

Example:

```python
text = "H"
print(text.ljust(5))
